# 允许访问图片文件
<FilesMatch "\.(jpg|jpeg|png|gif|webp|svg)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>

# 禁止访问其他文件
<FilesMatch "^\.">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# 禁止目录浏览
Options -Indexes
